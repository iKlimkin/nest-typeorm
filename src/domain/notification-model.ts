export type EmailData = {
  emailSettings: any;
  from: string;
  subject: string;
  message: string;
  email: string;
};

export type EmailEnvSettingTypes = {
  EMAIL_PASSWORD: string;
  EMAIL_USER: string;
  EMAIL_SERVICE: string;
};
